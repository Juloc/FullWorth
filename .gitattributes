# Shell/ops scripts must keep LF line endings so they run on the Linux deployment host
# regardless of the platform they were edited on (a CRLF shebang breaks with "bad interpreter").
*.sh text eol=lf
