# Routen, die heute kein Aufrufer im Frontend erreicht (#177).
#
# Das ist eine RATSCHE, keine Bestandsaufnahme: die Liste darf nur kuerzer werden.
# RouteReachabilityTests haelt beides fest - eine neue unerreichbare Route laesst den Test rot werden,
# und eine Route, die inzwischen einen Aufrufer hat, muss hier heraus.
#
# Nicht jede Zeile ist ein Fehler. Manches ist absichtlich ohne Oberflaeche:
#
#   /api/bootstrap/*                bedient der Auth-Host (FullWorth.Web), nicht die Anwendung
#   /api/coach/ask                  die kurze Frage ohne Unterhaltung; die Seite benutzt Unterhaltungen
#   /api/intelligence/brand-assets  die Adresse kommt aus dem Katalog (BrandCatalogEndpoints setzt
#                                   assetPath), und FullWorth.Web/Program.cs reicht sie durch - das
#                                   Frontend schreibt sie deshalb nirgends hin
#
# Erledigt: die Sammelbearbeitung. Es waren ZWEI Maschinen dafuer, beide fertig und beide ohne
# Aufrufer; die aeltere ist geloescht, die gebliebene haengt an der Auswahl, die es auf der
# Buchungsseite ohnehin gab. 264 Zeilen weniger, und die Oberflaeche zementiert keine Doppelung.
#
# Der Rest ist fertig gebaut und unerreichbar, und die groessten Brocken darunter sind:
#
#   /api/analytics/sankey           das Fluss-Diagramm der freien Auswertung. Der Rumpf lief lange
#                                   gar nicht - FinancialReconciliationMiddleware fing die Route vor
#                                   der Zuordnung ab. Die Middleware ist weg, der Aufrufer fehlt noch.
#   /api/saved-analyses/*           gespeicherte Auswertungen, volles CRUD
#   /api/account-experience/*       Darstellung und Reihenfolge der Konten
#   /api/category-merge             Kategorien zusammenfuehren
#
DELETE /api/budget-groups/{id:guid}
DELETE /api/saved-analyses/{id:guid}
GET /api/account-experience/group-appearances
GET /api/bank-capabilities
GET /api/category-intelligence/category-appearances
GET /api/category-merge/{sourceCategoryId:guid}/preview
GET /api/intelligence/brand-assets/{contentSha256}
GET /api/intelligence/digests/{id:guid}
GET /api/investments/net-worth-contribution
GET /api/merchants/resolve
GET /api/product-learning/category-suggestions
GET /api/purchase-analytics/by-merchant
GET /api/purchases/discount-analytics
GET /api/tax/profiles
POST /api/account-experience/reorder
POST /api/account-experience/{accountId:guid}/seen
POST /api/analytics/sankey
POST /api/bootstrap/accept-invite
POST /api/bootstrap/deactivate-user
POST /api/bootstrap/first-admin
POST /api/bootstrap/purge-user
POST /api/bootstrap/reactivate-user
POST /api/category-intelligence/bulk
POST /api/category-intelligence/learn
POST /api/category-merge/{sourceCategoryId:guid}
POST /api/coach/ask
POST /api/import/wealth-backup/validate
PUT /api/account-experience/{accountId:guid}/appearance
PUT /api/budget-groups/{id:guid}
PUT /api/category-intelligence/category-appearances/{categoryId:guid}
PUT /api/category-order
PUT /api/saved-analyses/{id:guid}
