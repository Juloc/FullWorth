{
  "//": "Development-only overrides. Loaded automatically when ASPNETCORE_ENVIRONMENT=Development (the plain-HTTP local dev stack). Production keeps the stricter appsettings.json values.",
  "RateLimits": {
    "//": "A single local user driving a widget-heavy dashboard fans out into many authenticated BFF calls; the production 600/min budget is easy to trip while clicking around locally. Give local testing generous head-room. Abuse protection is irrelevant on a private single-host dev stack.",
    "BrowserApi": { "PermitLimit": 20000, "WindowSeconds": 60, "QueueLimit": 0 },
    "Login": { "PermitLimit": 100, "WindowSeconds": 300, "QueueLimit": 0 }
  }
}
